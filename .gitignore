node_modules/
# Build output, generated by `npm run build`
dist/
# Local per-machine cache of Engram observation ids — never shared
.opencode/.persona-cache.json
# Plugin diagnostics log, local per machine
.opencode/persona.log
# Local tooling cache (skill-registry), not versioned
.atl/
# Role files are defined by each project that uses the plugin, not versioned here
harness/
# Local AI tooling caches
.claude/
.codegraph/
